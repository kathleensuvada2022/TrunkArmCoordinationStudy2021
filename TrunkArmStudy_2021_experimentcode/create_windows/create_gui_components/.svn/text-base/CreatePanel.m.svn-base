function panel = createPanel( position, figureHandle, title )

panel = uipanel('BorderType','etchedin',...
		...%'BackgroundColor',panelColor,...
        'Units','characters',...
        'Position',position,...
		'Parent',figureHandle,...
        'Title',title);