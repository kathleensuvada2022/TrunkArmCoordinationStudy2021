function listboxHandle = CreateListboxComponent( parent, position, string)

listboxHandle = uicontrol('Parent', parent,...
    'Style', 'listbox',...
    'Units','characters',...
    'Position', position,...
    'String',string,...
    'FontSize',12);
